DB_HOST=
DB_PORT=3306
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=
JWT_SECRET=
CORS_ORIGIN=http://localhost:3001

# Antes de arrancar contra datos reales:
# 1) Correr backend/migrations/001_add_timestamps_and_pass.sql
# 2) Correr backend/migrations/002_convert_set_to_enum.sql
# 3) Correr backend/migrations/003_add_soft_delete_contratos.sql
# 4) Correr backend/migrations/004_contratos_timestamps_default_db.sql (ya cubierta por la 005, se puede omitir si se corre la 005)
# 5) Correr backend/migrations/005_all_tables_timestamps_default_db.sql
# 6) (Opcional, recomendado) Correr backend/migrations/rehash-legacy-passwords.ts
